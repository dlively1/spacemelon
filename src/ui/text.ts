// Shared text styling. The whole UI (menus, HUD, banners, game-over panel) is
// rendered in one monospace family for its retro arcade look — keep it here so
// the typeface can be swapped in a single place instead of across every scene.
export const MONO_FONT = "Courier New, monospace";
